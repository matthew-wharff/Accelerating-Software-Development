EXECUTION POLICY — READ THIS FIRST

This is a STATIC audit. You are reading source code, not running it.

DO NOT run:
- python -m graph.pipeline, python main.py, or any pipeline entrypoint
- pytest (the user runs tests themselves)
- Any agent file directly (python agents/coder.py, etc.)
- Any script that makes API calls to Anthropic, GitHub, or e2b

DO NOT invoke:
- The GitHub MCP server
- Any tool that creates repos, commits, or pushes code
- The e2b sandbox

You may run ONLY these read-only commands:
- cat, head, tail, grep, rg, find, ls, tree
- ruff check (already run by user)
- python -c "import ast; ..." for static AST analysis

If you think you need to execute something to verify a claim, write 
the finding as "UNVERIFIED — would require running X" and move on.

You are doing a READ-ONLY audit of my multi-agent dev assistant codebase.

Ground truth documents (read these first):
- /context/project_guide_v2.md — full architecture and design rationale
- /context/DevAssistant_TaskList_v3.md — task-by-task spec with IDs (PRE-xx, MVP-xx, AGT-xx)
- /context/CONVENTIONS.md — coding standards
- /context/ARCHITECTURE.md — high-level system overview
- /context/shared_dependencies.md — cross-file contract manifest
- CLAUDE.md — project rules and "what NOT to do"

DO NOT edit any files. DO NOT suggest refactors. DO NOT write new code.

Your only job is to produce an AUDIT_REPORT.md at the project root with these sections:

1. SPEC ALIGNMENT
   For each agent in /agents/, compare against its corresponding AGT-xx 
   task in DevAssistant_TaskList_v3.md and the agent description in 
   project_guide_v2.md. Flag:
   - Missing functionality the spec requires
   - Extra functionality not in the spec (scope creep)
   - Wrong model assignment (Sonnet vs Haiku per CLAUDE.md table)
   - Reference the specific AGT-xx ID for each finding

2. CONVENTIONS COMPLIANCE
   Check every file in /agents/ and /graph/ against /context/CONVENTIONS.md.
   Flag specific violations with file:line references. Do not fix them.

3. CONTEXT ISOLATION VIOLATIONS
   Per CLAUDE.md "What NOT to Do" and project_guide_v2.md Section 4 
   (Context Management), flag any code where:
   - Coder receives full source code instead of signatures
   - State stores file contents instead of paths
   - Raw critic output flows to Coder (must go through Synthesis Agent)
   - print() is used instead of logger
   - Conversation history accumulates in LangGraph state
   - Generated code is held in state rather than written to /output/

4. DEAD CODE / HALLUCINATIONS
   Flag any imports that don't resolve, function calls to non-existent 
   methods, or references to files/modules that don't exist on disk.

5. CONSISTENCY
   Flag inconsistencies BETWEEN files (e.g. coder.py expects a state 
   field that schema.py doesn't define, or shared_dependencies.md 
   references types that aren't actually exported).

6. ARCHITECT-CODER LOOP INTEGRITY (per AGT-03B)
   Specifically verify:
   - Task failure escalation triggers at >= 3 failures
   - revision_count is capped at 2
   - Coder receives scoped context per task, not full spec
   - Interface extraction happens after each task completes

For each finding:
- Severity (high/med/low)
- File path and line number
- What's wrong
- What the spec says it should be (cite the source doc and section/task ID)
- NO suggested fixes — just observations

RUNTIME STATE FILES (read-only, never edit, never suggest edits to):
- /context/shared_dependencies.md — owned by the Architect Agent, 
  populated during pipeline runs
- /output/** — generated by the Coder Agent during pipeline runs
- Any *.json file under /context/ (e.g. task_queue.json) — runtime artifacts

These files are PIPELINE OUTPUTS, not source code. Audit them only to 
verify they're consistent with the source code that produces them. Do 
not propose edits, even as suggestions.